# 손상 데이터 복구 참고표 (Recovery Reference)

## 주요 대학 데이터 복구 예시

### 1. 고려대학교 (Korea University)
```javascript
// ❌ 손상된 코드
"고려대학교": {
  "경영?과": ["경영?과"],
  "문과?과": ["?문?학과", "철학?과", ...],
  "?명과학?과": ["?명과학부", "?명공학부", ...],
}

// ✓ 정상 코드
"고려대학교": {
  "경영대학": ["경영학과"],
  "문과대학": ["국어국문학과", "철학학과", ...],
  "에너지과학포함대학": ["에너지과학부", "에너지공학부", ...],
}
```

### 2. 서울대학교 (Seoul National University)
```javascript
// ✓ 이미 정상 (Unicode 이스케이프 사용)
"\uc11c\uc6b8\ub300\ud559\uad50": {
  // 정상 상태 유지
}
```

### 3. 세종대학교 (Sejong University)
```javascript
// ❌ 손상된 코드
"?세?교": {
  "문과?과": ["?문?과", "중어중문?과", ...],
  "?경?과": ["경제??", "?용?계?과"],
}

// ✓ 정상 코드
"세종대학교": {
  "문과대학": ["국어국문과", "중어중문학과", ...],
  "경상대학": ["경제학과", "응용계산학과"],
}
```

### 4. 한양대학교 (Hanyang University)
```javascript
// ❌ 손상된 코드
"?양?교": {
  "공과?과": ["건축??", "건축공학부", ...],
  "?문과학?과": ["?문??과", "중어중문?과", ...],
}

// ✓ 정상 코드
"한양대학교": {
  "공과대학": ["건축학과", "건축공학부", ...],
  "인문과학대학": ["국어국문학과", "중어중문학과", ...],
}
```

---

## 손상 패턴 분류 및 고정 규칙

### 패턴 A: 단일 문자 손상
```
손상 패턴: [문자]?[문자] → [문자][정상문자][문자]
예시: "경제?과" → "경제학과"
      "?학?과" → "경학과" (주의: 전후 문맥 필요)
```

### 패턴 B: 연속 문자 손상
```
손상 패턴: ?[여러문자]? → 정상문자열
예시: "?세?교" → "세종대학교"
      "?경?과" → "경제학과"
```

### 패턴 C: 더블 손상
```
손상 패턴: ?씪??떒? → 정상문자
예시: "?쒖슱?€?숆탳" → "서울대학교"
```

---

## 일반적인 한글 학과명/계열명 복구 매핑

| 손상된 텍스트 | 정상 텍스트 | 분류 |
|------------|---------|------|
| ?경? | 경제 | 학과 |
| ?과? | 과학 | 학과 |
| 공과?과 | 공과대학 | 단과대학 |
| 문과?과 | 문과대학 | 단과대학 |
| ?명과학 | 에너지과학 | 학과 |
| 경영?과 | 경영학과 | 학과 |
| ?범?과 | 사범대학 | 단과대학 |
| 간호?과 | 간호학과 | 학과 |
| ?보?과 | 정보학과 | 학과 |
| 보건과학?과 | 보건과학대학 | 단과대학 |

---

## 복구 가능도 판정 기준

### 복구 쉬움 (Easy) ✓
```
기준: 손상 문자 1-2개, 명확한 대학/학과명
예: "세?대학교" → "세종대학교"
    "경?학과" → "경제학과"
복구율: 95%+
```

### 복구 중간 (Medium) ⚠️
```
기준: 손상 문자 3-5개, 문맥으로 추정 가능
예: "?종?학교" → "세종대학교"
    "공?대?적" → "공학대학" (단, "공개대학"일 수도 있음)
복구율: 70-90%
```

### 복구 어려움 (Hard) ✗
```
기준: 손상 문자 5개+, 다중 의미 가능
예: "??대?교" → ? (세종대학교 또는 다른 대학)
    "?과??" → ? (여러 학과 가능)
복구율: 50% 이하
```

---

## 손상된 메시지 복구 예시

### 콘솔 메시지
```javascript
// ❌ 손상
console.log("시트(?) 헤더 발견:", h, "RCol:", finalRCol, "TCol:", finalTCol);

// ✓ 정상
console.log("시트(이름) 헤더 발견:", h, "RCol:", finalRCol, "TCol:", finalTCol);
```

### 경고 메시지
```javascript
// ❌ 손상
showAppMessage("?돌?림?습?다.", "warning");

// ✓ 정상
showAppMessage("되돌릴 기록이 없습니다.", "warning");
```

### 알림 메시지
```javascript
// ❌ 손상
alert("紀€€? ??€??? ?????€???? ??€???.");

// ✓ 정상
alert("모든 데이터를 초기화하고 저장된 정보를 삭제합니다.");
```

---

## 복구 작업 체크리스트

### 대학 데이터 검증
- [ ] 고려대학교: 15개 단과대학 정상 표시
- [ ] 서울대학교: Unicode 이스케이프 확인
- [ ] 연세대학교: 정상 표시
- [ ] 이화여자대학교: 정상 표시
- [ ] 서강대학교: 정상 표시
- [ ] 한양대학교: 정상 표시
- [ ] 기타 50+개 대학: 샘플 검증

### 메시지 검증
- [ ] 콘솔 메시지: 모두 한글 정상 표시
- [ ] 경고/알림: 모두 한글 정상 표시
- [ ] UI 텍스트: 모두 한글 정상 표시

### 기능 검증
- [ ] 대학 선택 드롭다운: 정상 작동
- [ ] 학과 선택 드롭다운: 정상 작동
- [ ] AI 분석: 정상 실행
- [ ] 파일 업로드: 정상 처리

---

## 참고 데이터 (정상 파일에서 추출)

**universityEvalCriteria.json에서 정상 확인된 대학:**
```json
{
  "서울대학교": { ... },
  "고려대학교": { ... },
  "서강대학교": { ... },
  "한양대학교": { ... }
}
```

이 파일의 대학명 형식을 복구의 템플릿으로 사용 가능함.

---

## 자동 복구 스크립트에서 놓친 부분

### 수동으로 확인해야 할 사항
1. **70-80개 대학의 학과명**: 자동 스크립트로는 완벽 복구 불가능
2. **고급 메시지**: 콘솔의 복잡한 한글 메시지
3. **특수 문자**: ·, —, · 등의 한글 특수 문자

### 권장 접근법
```
자동 복구 → 수동 검증 → 부분 수정 → 테스트
```

---

**마지막 주의**: 
> 손상 문자(?)가 남아있다면 파싱이 실패할 수 있으므로
> 최종 검증은 필수입니다!
